services:
  mongodb:
    image: mongo:8
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: agent_sessions
    volumes:
      - mongodb_data:/data/db

volumes:
  mongodb_data:
