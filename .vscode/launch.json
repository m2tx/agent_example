{
    "configurations": [
        {
            "name": "Launch Go with .env",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/cmd/server/",
            "envFile": "${workspaceFolder}/.env",
            "args": []
        }
    ]
}